services:
  integration-test:
    container_name: integration-test
    pid: "host"
    build:
      context: .
      dockerfile: integration-test/Dockerfile
    depends_on:
      app:
        condition: service_healthy
    networks:
      app_network:
        aliases:
          - test.lvh.me


networks:
  app_network:
    external: false
